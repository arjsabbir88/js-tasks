// find maximum value between 3 number

let x = 12, y = 25, z = 18;

let result = x>y && x>z ? "x is the maximum value" 
            : y>x && y>z ? "y is the maximum value" 
            : "z is the maximum value";

console.log(result)